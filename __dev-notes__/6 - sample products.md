# Notes

## Sample Products

we want to list some products to list them as sample data from a typescript file, so we'll have some UI in place to display products.

There's a sample data file he provided.

put the `db` folder in the root directory. put the sample product images in the `public/images/sample-products/` folder.

The `sample-data.ts` file in the `db` folder is a json file with JSON data for products.

There are six products in there.

Added the names to the product card to start, and then we'll develop the rest.

We also enabled the ability to limit the number of products shown; in this case to 4.
